
# Command Line Interface (CLI) for Camera Worker


### 1. Command to run the camera worker with specific tenant and camera IDs:
```bash
python main.py --tenant_id tenant_1 --camera_id cam_01 --enable_livekit
```